package com.geektime.lesson;

/**
 * 类描述:     [algorithm019] Java递归模板
 * 创建人:     [jkdong]
 * 创建时间:   [2021/1/2 8:52 下午]
 * 版本:       [v1.0]
 */
public class JavaRecTemplate<Object> {


    private static final int MAX_LEVEL = 1;
    private Object newParam;

   /* public void recur(int level, int param){
        // terminator
        if(level > MAX_LEVEL){
            // process result
            return;
        }
        // process current logic
        process(level, param);
        // drill down
        recur(level: level + 1, newParam);
    }

    private void process(int level, int param) {
    }*/
}
